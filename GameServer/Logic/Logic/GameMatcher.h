#pragma once

namespace FPLogic
{
	class GameMatcher
	{
	public :

		GameMatcher() {}
		~GameMatcher() {}

		// RequestMatch : 매칭 큐에 집어 넣어 매칭 상대를 요청하는 메소드.
		// TODO :: 일단 기다리는 사람이 두 명 이상이면 무조건 매칭.
		// 나중에 매칭 랭킹 시스템 도입.

	private :

		// 매칭을 기다리는 큐를 가지고 있음.

	};
}
